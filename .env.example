# Optional: enables the "Betting Odds" toggle in the app.
# Get a free key at https://the-odds-api.com/ (500 requests/month tier is plenty).
# When unset, the app runs the pure model and the toggle shows a hint to add a key.
ODDS_API_KEY=
